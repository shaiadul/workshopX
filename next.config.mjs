/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['cdn.pixabay.com', 'i.pravatar.cc', 'images.unsplash.com', 'www.shutterstock.com','plus.unsplash.com'],
  },
};

export default nextConfig;
